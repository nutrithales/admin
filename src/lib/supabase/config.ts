/**
 * Public endpoint for the Supabase project used by the admin and patient apps.
 *
 * Keeping this public (non-secret) URL here prevents an accidentally copied
 * Vercel deployment URL from breaking authentication. Keys remain exclusively
 * in environment variables.
 */
export const SUPABASE_URL = "https://xwihrxinweeadtcouhoo.supabase.co";
